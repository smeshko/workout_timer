import Foundation

public extension ExerciseSet {
  static func jumpingJacks(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .jumpingJacks, duration: duration)
  }
  
  static func pushUps(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .pushUps, duration: duration)
  }
  
  static func crissCross(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .crissCross, duration: duration)
  }
  
  static func doubleUnder(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .doubleUnder, duration: duration)
  }
  
  static func boxerStep(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .boxerStep, duration: duration)
  }
  
  static func recovery(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .recovery, duration: duration)
  }

  static func runInPlace(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .runInPlace, duration: duration)
  }
  
  static func freestyle(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .freestyle, duration: duration)
  }
  
  static func highKnees(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .highKnees, duration: duration)
  }
  
  static func frontToBack(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .frontToBack, duration: duration)
  }
  
  static func simpleJump(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .simpleJump, duration: duration)
  }
  
  static func sideToSide(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .sideToSide, duration: duration)
  }
  
  static func heavyRopeJumping(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .heavyRopeJumping, duration: duration)
  }
  
  static func pullUps(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .pullUps, duration: duration)
  }

  static func shadowBoxing(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .shadowBoxing, duration: duration)
  }
  
  static func boatToLowBoat(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .boatToLowBoat, duration: duration)
  }
  
  static func oneArmShoulderTouches(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .oneArmShoulderTouches, duration: duration)
  }
  
  static func bicycles(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .bicycles, duration: duration)
  }

  static func plank(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .plank, duration: duration)
  }
  
  static func lyingTStretch(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .lyingTStretch, duration: duration)
  }
  
  static func figureFourStretch(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .figureFourStretch, duration: duration)
  }
  
  static func lyingHamstringStretch(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .lyingHamstringStretch, duration: duration)
  }

  static func quadRockers(_ duration: TimeInterval) -> ExerciseSet {
    ExerciseSet(exercise: .quadRockers, duration: duration)
  }
}
